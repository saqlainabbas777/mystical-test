import React from "react";
import styles from "./AcceptButton.module.css";

const AcceptButton = () => {
  return <button className={styles.accept_button}>Accept Swap</button>;
};

export default AcceptButton;
